# 2026-06-30 — GE2-R11 real inbound Gateway smoke result

Final classification: `GE2_R11_REAL_INBOUND_BLOCKED_P2_PASS_ONLY`.

R11 did not pass P3. R10 remains the highest valid proof level: `GE2_R10_ADAPTER_FIXTURE_TELEGRAM_WEBUI_HELP_STATUS_RUN_PASS_P3_PENDING`.

What was proven in R11:
- Real inbound route families were identified.
- Telegram real inbound path: Telegram Bot API update -> `createTelegramBot` / `registerTelegramNativeCommands` -> `bot.handleUpdate` in Gateway -> `matchPluginCommand(commandBody)` -> `executePluginCommand(...)` -> Telegram reply/send path with sent-message ledger at `/home/stickai/.openclaw/agents/main/sessions/sessions.json.telegram-sent-messages.json`.
- Telegram direct session key: `agent:main:telegram:direct:8495203551`.
- WebUI/Gateway path: live Gateway RPC `chat.send` -> dispatch/get-reply command routing -> `commands-handlers.runtime` `handlePluginCommand` / `matchPluginCommand(commandBodyNormalized)` -> `executePluginCommand(...)` -> chat transcript/live session event.
- `chat.send` minimum params: `sessionKey`, `message`, `idempotencyKey`.
- Command visibility stayed correct: `/ge2` count exactly 1 for Telegram and webchat; fake command absent; existing commands `pair`, `dreaming`, `phone`, `voice` preserved.

Safe live-probe findings:
- Rendered browser WebUI could not be used because the isolated host browser failed to start: `No supported browser found (Chrome/Brave/Edge/Chromium)`.
- Live Gateway `chat.send` accepted `/ge2 help` on `agent:main:main`, but the transcript response was the auth gate: `⚠️ This command requires authorization.` This was `gateway-injected` with zero token usage, so no model/chat fallthrough was observed, but GE2 native execution was not proven.
- Retrying `chat.send` with Gateway token did not change command-owner scope.
- An internal `chat.send` against the current Telegram direct session was stopped and not counted as P3 because it is not real external Telegram inbound and polluted the active transcript.

Boundaries honored:
- no production patch
- no Gateway restart
- no rollback
- no cron closeout apply
- no promotion
- no spoofed inbound Telegram
- no `/ge2 run` because `/ge2 help` and `/ge2 status` did not pass on a live P3 surface

Artifacts:
- JSON report: `sharedspace/runtime-kernel-validation/ge2/ge2_r11_real_inbound_gateway_smoke/GE2_R11_PHASE1_REAL_INBOUND_ENTRYPOINTS_AND_BLOCKERS_20260630.json`
- Markdown report: `sharedspace/runtime-kernel-validation/ge2/ge2_r11_real_inbound_gateway_smoke/GE2_R11_PHASE1_REAL_INBOUND_ENTRYPOINTS_AND_BLOCKERS_20260630.md`
- JSON SHA256: `ac21ae5f87d21741c8fd79acde914704c0ba48fa95d088a8479fe5427d8322d9`

Next valid path:
1. For Telegram P3: Stick/operator sends real Telegram direct commands in this chat: `/ge2 help`, `/ge2 status`, then only after those pass `/ge2 run r11-telegram-real-inbound-smoke`, `/ge2 status <run_id>`, `/ge2 artifacts <run_id>`.
2. For WebUI P3: use a real rendered WebUI/browser client that carries owner/operator command context, or repair/identify the legitimate owner-scoped WebUI handshake. Do not spoof sender ownership.
3. If both pass, next classification target is `GE2_R11_REAL_INBOUND_GATEWAY_TELEGRAM_WEBUI_PASS_PROMOTION_PENDING`; then proceed to `GE2_R12_PROMOTION_READINESS_PACKET_PENDING`, not immediate promotion.
