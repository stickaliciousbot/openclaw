# 2026-06-30 — GE2-R8 intake and collaboration rule

Stick clarified a standing collaboration preference during GE2-R8 planning:

- Do not treat Stick's prompts as the ceiling of the build. Treat them as operator guidance from an outside observer.
- Stick expects Stickbot to contribute expert judgment: identify missing pieces, augment prompts, propose revised builds, and make the implementation stronger when safe.
- If a prompt is incomplete, say what is missing and provide the revised build/plan rather than passively waiting, unless the prompt explicitly sets a gate.
- For this GE2-R8 prompt specifically, Stick instructed: read until the word `banana`, saving bits to memory and implementation notebooks before implementing. Since `banana` has not appeared yet, implementation must not start.

Current GE2 state carried into R8:

- Current classification: `GE2_R7_COMMAND_REGISTRY_VISIBILITY_PASS_HELP_STATUS_PENDING`
- Health re-check PASS: PID `303370 -> 307081`; service/runtime/listener/connectivity/admin all green; fatal/missing-module/mixed-runtime scan count 0; rollback no.
- Live commands.list gates PASS: `/ge2` present exactly once in default, telegram/both, and telegram/text; total command count 61; existing commands `pair`, `dreaming`, `phone`, `voice` preserved; fake absent.
- Live `/ge2 help/status` not run because no non-model direct native-command execution route was identified.
- Async direct RPC probe found only `commands.list`; addendum at `sharedspace/runtime-kernel-validation/ge2/ge2_r7_source_snapshot_repair_plan/r7_async_direct_command_rpc_probe_addendum_20260630.md`.

R8 intended goal from partial prompt:

- Discover/prove a safe, non-model native execution path for `/ge2 help` and `/ge2 status` without production mutation, Gateway restart, rollback, cron apply, plugin-manager bridge patch, outbound bot spoofing, or model/chat spoofing.
- Trace Telegram and WebUI native command execution paths.
- Determine whether an internal harness/test entry point can call the exact production matcher/handler.
- Candidate preferred option starts with Option A: installed-dist native handler harness importing production-installed dist modules locally.

Gate:

- Do not implement until the remainder of the prompt arrives and `banana` is seen.
