# 2026-06-30 — GE2-R10 adapter fixture proof intake

User authorized proceeding from R9 P1 PASS to R10 P2 adapter fixture proof for Telegram and WebUI.

Starting classification: `GE2_R9_NATIVE_HANDLER_RUN_STATUS_ARTIFACTS_PASS_P2_PENDING`.

R10 goal: prove adapter-shaped fixture paths for Telegram and WebUI normalize `/ge2` into the actual native command matcher/handler and avoid model/chat fallthrough. This is P2 only, not P3 real inbound Gateway proof.

Commands per surface:

Telegram fixture:
1. `/ge2 help`
2. `/ge2 status`
3. `/ge2 run r10-telegram-adapter-fixture-smoke`
4. `/ge2 status <telegram_run_id>`
5. `/ge2 artifacts <telegram_run_id>`

WebUI fixture:
1. `/ge2 help`
2. `/ge2 status`
3. `/ge2 run r10-webui-adapter-fixture-smoke`
4. `/ge2 status <webui_run_id>`
5. `/ge2 artifacts <webui_run_id>`

Hard gates per surface:
- fixture enters adapter/native command path
- matches native `/ge2`
- pluginId `ge2-native`
- `continueAgent:false`
- no model/chat fallthrough
- native GE2 response
- durable ledger entry
- milestones
- artifact exists
- SHA matches
- status/artifacts retrieve exact run_id
- bounded/compressed command responses

Assumed cross-surface parity gates because user prompt cut off after heading:
- both surfaces produce distinct run_ids
- both write distinct artifacts with matching SHA
- both preserve `/ge2` duplicate count 1, fake absent, existing commands preserved
- Gateway health remains green
- no production mutation, no Gateway restart, no rollback, no cron retry, no promotion

Boundaries: P2 only; no P3 overclaim; no outbound bot spoofing; no real inbound live Gateway command path; no production code/config/service mutation.
