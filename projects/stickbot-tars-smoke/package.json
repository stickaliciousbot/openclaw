{
  "name": "stickbot-tars-smoke",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-only Stickbot/TARS voice smoke: browser text+mic UI, OpenClaw bridge, XTTS voice, memory/context logging.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/config.js && node --check safety/network-policy.js && node --check safety/audio-path-policy.js && node --check safety/limits.js && node --check safety/origin-policy.js && node --check safety/csrf.js && node --check public/app.js && node --test test/*.test.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
