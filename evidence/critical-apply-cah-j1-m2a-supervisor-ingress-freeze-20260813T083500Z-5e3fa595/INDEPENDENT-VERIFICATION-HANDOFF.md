# Independent verification handoff

Independently rehash the immutable inputs, design, `ARTIFACT-SHA256.json`, and every evidence manifest entry. Run `python3 -m py_compile validate_m2a_freeze.py test_validate_m2a_freeze.py`, then the validator and mutation tests from a clean copied evidence root. Confirm 26 states, 26 complete transitions, 52 transition-specific crash vectors, 7 concurrency/fencing vectors including 2/5/20 and overlaps, 8 ACK vectors, and all 12 mutation rejections. Run the frozen bounded privacy scanner SHA-256 `ce38bf7811561c096bfe72ef12fc799712986d77ff86cb62b8e099eedc481115` over the exact root and design copy. Confirm zero effects and no M2B start. Write a fresh independent root and terminal `PASS_VERIFIED` or exact HOLD blockers; do not alter this construction root.
