# M6 Worker Model and Fallback Policy

Status: `PASS_M6_WORKER_MODEL_AND_FALLBACK_POLICY_DEFINED`

Initial worker: `openai-codex/gpt-5.5` as `execution_worker` only. Route authority is false. Fallbacks must preserve contract version, no_send delivery, observe_only authority, terminal closeout, VerifiedRoute, ContractEnvelope, and capability manifest requirements.
